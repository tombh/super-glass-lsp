name: CI

on:
  pull_request:
  push:


jobs:

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.4
      - name: Set up Python
        uses: actions/setup-python@v2
      - name: Install Poetry
        uses: abatilo/actions-poetry@v2
        with:
          poetry-version: 1.2.1
      - name: Install dependencies
        run: poetry install
      - name: Flake8
        run: poetry run flake8
      - name: Mypy type checker
        run: poetry run mypy super_glass_lsp tests
      - name: Black formatting
        run: poetry run black super_glass_lsp tests
  test:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10"]
        # os: [ubuntu-latest, macos-latest, windows-latest]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Install external tool dependencies
        run: |
          sudo apt-get install jq fzf mypy
          npm install --global markdownlint
      - uses: actions/checkout@v2.3.4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Poetry
        uses: abatilo/actions-poetry@v2
        with:
          poetry-version: 1.2.1
      - name: Install project dependencies
        run: poetry install
      - name: Run tests
        run: poetry -vvv run python -m pytest --log-cli-level=DEBUG
      - name: LSP server debug logs (if tests fail)
        if: ${{ failure() }}
        run: cat ./lsp-server-test.log || echo "No log file"

